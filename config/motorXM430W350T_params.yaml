/**:
  ros__parameters:

    # ── Modelo torque→corriente Dynamixel XM430-W350-T (OpenMANIPULATOR-X) ──
    # identify_motor_model.py — 2026-06-24  (alpha compartida + Fv,Fc>=0)
    # Fuente: data/diagnostics/sinusoidal/hw_sin_torque_11.csv
    # tau por RNEA (Pinocchio + open_manipulator_x.urdf); vel='sg' acc='ref'
    # Modelo: I[i] = alpha[i]·tau[i] + Fv[i]·dq[i] + Fc[i]·tanh(dq[i]/eps) + I_offset[i]
    # R² por joint: J1=0.799, J2=0.835, J3=0.811, J4=0.781
    # corr(tau,I): J1=-0.10, J2=0.75, J3=0.10, J4=0.44   (|corr|<0.3 → alpha poco fiable, se apoya en la compartida)
    #
    # Unidades:
    #   motor_alpha            [ticks/N·m]
    #   motor_Fv               [ticks/(rad/s)]
    #   motor_Fc               [ticks]
    #   motor_I_offset         [ticks]
    #   motor_epsilon_friction [rad/s]

    motor_alpha:             [152.3076, 152.3076, 152.3076, 152.3076]    # J1 J2 J3 J4
    motor_Fv:                [6.7797, 0.0000, 0.0000, 0.0000]
    motor_Fc:                [4.1414, 22.3124, 28.3844, 9.4338]
    motor_I_offset:          [-3.9806, -1.3883, -4.7173, -0.0710]
    motor_epsilon_friction:   0.0500
